import timeDefault from './timeDefault'
import createAxios from './createInstance'
import setFileToBase from './setFileToBase'
import useJWT from './useJWT'
import validateForm from './validateForm'

const config = {
    timeDefault,
    createAxios,
    setFileToBase,
    useJWT,
    validateForm,
}

export default config
